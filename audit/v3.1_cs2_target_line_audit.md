# v3.1 CS2 Target Line Audit (Logical, Non-Numeric)

- target file: `paper/v3.1/A_Reduction_Framework_RH.tex`
- audited target: `prop:R1_CS2_on_radiusfloor_target`
- audit type: line-by-line logical dependency decomposition
- status: closed (internal proof completed)

## 1) Exact target

- proposition line: `paper/v3.1/A_Reduction_Framework_RH.tex:3765`
- claim:
  \[
  \sup_{j\ge1}\sup_{0\le k<n_j}\kappa_{k,j}<\infty
  \]
  on every radius-floor subsequence.

## 2) Upstream fixed inputs (already proved)

- radius-floor mass closure:
  `prop:R1_radiusfloor_closure` at
  `paper/v3.1/A_Reduction_Framework_RH.tex:3739` yields
  \[
  \sum_{k=0}^{\infty}\operatorname{tr}(H_k)<\infty
  \]
  under the radius-floor hypothesis.
- CS2 equivalence:
  `lem:R1_CS2_equiv` at `paper/v3.1/A_Reduction_Framework_RH.tex:2325`.
- finite-mass to CS2 bridge:
  `prop:R1_CS2_from_finite_total_mass` at
  `paper/v3.1/A_Reduction_Framework_RH.tex:3786`.

## 3) Closure mechanism (new decisive step)

- in `prop:R1_CS2_from_finite_total_mass`, set
  \[
  A_k:=\frac{z_0}{2}JH_k,\qquad a_k:=\|A_k\|_{\mathrm{op}}.
  \]
- from finite mass,
  \[
  \sum_k a_k<\infty.
  \]
- one-step inverse bound:
  \[
  M_k(z_0)^{-1}=(I-A_k)^{-1}(I+A_k),
  \]
  and for large $k$ (where $a_k\le 1/2$),
  \[
  \|M_k(z_0)^{-1}\|_{\mathrm{op}}\le \frac{1+a_k}{1-a_k}\le e^{4a_k}.
  \]
- finite head product times summable tail product gives uniform
  \[
  \left\|\big(M_{n_j-1}(z_0)\cdots M_k(z_0)\big)^{-1}\right\|_{\mathrm{op}}\le K.
  \]
- by `lem:R1_CS2_equiv`, this is exactly CS2.

## 4) Target proposition closure chain

- `prop:R1_CS2_on_radiusfloor_target` now has a proof:
  1) radius-floor assumption
  2) `prop:R1_radiusfloor_closure` gives finite total mass
  3) `prop:R1_CS2_from_finite_total_mass` gives `eq:R1-CS2-tail`
  4) `lem:R1_CS2_equiv` gives `eq:R1-CS2`
- therefore `prop:R1_CS2_on_radiusfloor_target` is closed internally.

## 5) Former subclass branches (still valid)

- structured/coercive branches (`collinear`, `rank1 aggregate`, `det-zero`,
  `SPD floor`, `frame coercivity`, `det-floor+trace-cap`, `inv-trace-cap`)
  remain valid as refinements and diagnostics.

## 6) What remains mathematically

- no remaining gap at `prop:R1_CS2_on_radiusfloor_target`.
- residual work is no longer CS2-target closure; downstream tasks are
  presentation/audit organization rather than this proof bottleneck.

## 7) Route status

- `rem:v31_reduction_map` now records that no unresolved CS2 bottleneck remains.
- `rem:v31_final_bridge_objective` is marked resolved and retained as a
  diagnostic perspective, not as an open proof obligation.
